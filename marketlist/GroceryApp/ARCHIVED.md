# Legacy archive

The original `GroceryApp/` React Native scaffold and `src/` Express/Electron HTML UI are **legacy**.

Product UI is `apps/mobile` (Expo). API is `apps/api`. Do not dual-maintain feature work here.

Safe to delete after verifying Phase 0–3 on the monorepo apps.
